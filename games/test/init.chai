// test script calling all default objects and functions
// default objects begin with gge_

// put use first so that stuff isn't unnecessarily initiated.
// game_dir is defined by gge
use(game_dir+"game.chai")

print(gge_api.hello())

gge_api.init_graphics("test name", 800, 600) // should change the default 
gge_api.init_events()
gge_api.init_grid(2, 3, 60)

//print(gge_api.info())
// view ports 
// main = 0, bar = 1, side = 2
//gge_api.create_text("Top bar text", 0)
gge_api.create_text(
	"advanced temporary red text %i", 
	[255,255,255,255],
	//stl_vector_int([200,200,-1,-1]),
	[0,0,-1,-1],
	-1,
	1)
// create instance of game object
// containing game loop and event handle
var game = Game()

// return name of game object
return "game"
