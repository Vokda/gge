// class
if(class_name == "graphics")
{
	module_type = GRAPHICS;
	module = moduler[module_type];
}
// member function
if(function_name == "clear_screen")
{
	arg = nullptr;
}

switch(module_type)
{
	default:
		{
			std::string s = "module_type " + GGE_module::get_module_name(module_type) + " not recognized!";
			throw invalid_argument(s);
		}
}
