#pragma once


struct Tile_coordinate
{
    // maybe we'll need it some day
    //virtual Tile_coordinate& operator+(Tile_coordinate const& other) = 0;
    
    virtual ~Tile_coordinate() = default;
};